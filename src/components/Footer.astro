---
const year = new Date().getFullYear();
const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : `${rawBase}/`;
const withBase = (p: string) => `${base}${p}`.replace(/\/{2,}/g, '/');
---

<footer class="site-footer">
  <div class="container site-footer__inner">
    <div class="site-footer__brand">
      <span class="brand">Infinita</span>
      <p class="site-footer__tagline">Sovereign · Disciplined · Enduring</p>
    </div>

    <nav class="site-footer__nav" aria-label="Footer">
      <a href={withBase('')}>Home</a>
      <a href={withBase('about')}>About</a>
      <a href={withBase('contact')}>Contact</a>
    </nav>

    <div class="site-footer__meta">
      <p>&copy; {year} Infinita — a privately held capital platform.</p>
      <p class="site-footer__disclaimer">
        Infinita is not a fund nor a financial product company. This website is
        for informational purposes only and does not constitute an offer,
        solicitation, or investment advice.
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--ink);
    color: var(--muted-dark);
    padding-block: clamp(3rem, 6vw, 4.5rem);
    font-size: 0.95rem;
  }
  .site-footer__inner {
    display: grid;
    gap: 2.5rem;
    grid-template-columns: 1.5fr 1fr;
    align-items: start;
  }
  .brand {
    font-family: var(--font-serif);
    font-size: 1.4rem;
    letter-spacing: 0.12em;
    color: var(--paper);
    text-transform: uppercase;
  }
  .site-footer__tagline {
    margin: 0.75rem 0 0;
    color: var(--gold);
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.24em;
    font-size: 0.72rem;
    font-weight: 400;
  }
  .site-footer__nav {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }
  .site-footer__nav a {
    color: var(--muted-dark);
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-size: 0.78rem;
    font-weight: 500;
  }
  .site-footer__nav a:hover { color: var(--gold); }
  .site-footer__meta {
    grid-column: 1 / -1;
    border-top: 1px solid var(--line-dark);
    padding-top: 1.75rem;
    color: #8b8b8b;
    font-size: 0.82rem;
  }
  .site-footer__meta p { margin: 0 0 0.4rem; }
  .site-footer__disclaimer { max-width: 60ch; }

  @media (max-width: 560px) {
    .site-footer__inner { grid-template-columns: 1fr; }
  }
</style>
